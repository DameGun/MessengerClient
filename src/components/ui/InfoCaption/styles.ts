import { defineStyleConfig } from '@chakra-ui/react';

export const infoCaptionStyle = defineStyleConfig({
  baseStyle: {
    backgroundColor: 'blackAlpha.600',
    color: 'white',
    borderRadius: 50,
    p: 2,
    px: 5,
  },
});
